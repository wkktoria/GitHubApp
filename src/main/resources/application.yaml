spring:
  application:
    name: GitHubApp
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:54321}/${DB_NAME:postgres}
    username: ${DB_USERNAME:user}
    password: ${DB_PASSWORD:admin}
  sql:
    init:
      mode: always